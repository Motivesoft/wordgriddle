meta {
  name: upload empty
  type: http
  seq: 6
}

post {
  url: http://{{HOST}}/api/bonus/upload
  body: multipartForm
  auth: none
}

body:multipart-form {
  file: @file(C:\Projects\GitHub\wordgriddle\test\data\empty.txt)
}

assert {
  res.status: eq 422
}

tests {
  test("Should return the status", function() {
    const data = res.getBody();
    expect(data).to.have.property('message');
  });
}
